server:
  port: 7777

logging:
  level:
    org:
      springframework:
        boot: INFO
    pl:
      futurecollars:
        invoicing: INFO
        invoicing.db: DEBUG

spring:
  profiles:
    active: file
  flyway:
    enabled: false
  security:
    user:
      name: arek
      password: arson

invoicing-system:
  allowedOrigins: http://localhost:4200
  csrf:
    disable: true